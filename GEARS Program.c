http://thiagomachado.net.br/downloads/jogo_da_velha.alg

char tabela [3];
char castel [3];
boolean jogou = falso;
 
for (stat = 0;tabela < 3;++tabela)
	if (v[stat] = 2) {
		for (castel = 0;castel < 3;++castel)
			if (tabela [stat,castel]= " ") && (jogou = falso) {
				pc = tabela[stat,castel]; 
				jogou = true;
				
			}
	}